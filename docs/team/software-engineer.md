# Software Engineer Role & Responsibilities

- Writes clean, modular, and maintainable Python 3.12+ code.
- Adheres strictly to the architectural blueprint in `docs/spec.md`.
- Implements comprehensive type hints passing `mypy`.
- Ensures zero semantic rewriting in dictation and strict parameter validation in control mode.
- Avoids blocking the PySide6 UI event loop by leveraging asynchronous worker threads.
